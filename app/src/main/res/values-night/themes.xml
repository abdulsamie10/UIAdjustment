<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidAppExercise" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Set the status bar color to transparent for light mode -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Base.Theme.AndroidAppExercise.Dark" parent="Base.Theme.AndroidAppExercise">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <!-- Set the status bar color to transparent for dark mode -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.AndroidAppExercise" parent="Base.Theme.AndroidAppExercise" />
    <style name="Theme.AndroidAppExercise.Dark" parent="Base.Theme.AndroidAppExercise.Dark" />
</resources>
